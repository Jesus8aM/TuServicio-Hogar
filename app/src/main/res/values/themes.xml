<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MyApplicationFeria"
        parent="Theme.MaterialComponents.Light.NoActionBar">

        <!-- Colores principales -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secundarios -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Fondo -->
        <item name="android:windowBackground">@color/background</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/primary_dark</item>

        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/white</item>

        <!-- Cards -->
        <item name="materialCardViewStyle">@style/AppCardStyle</item>

        <!-- Botones -->
        <item name="materialButtonStyle">@style/AppButtonStyle</item>

    </style>

    <style name="Theme.MyApplicationFeria.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyApplicationFeria.AppBarOverlay"
        parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="Theme.MyApplicationFeria.PopupOverlay"
        parent="ThemeOverlay.MaterialComponents.Light" />

    <!-- CARD MODERNA -->

    <style name="AppCardStyle"
        parent="Widget.MaterialComponents.CardView">

        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
        <item name="strokeColor">@color/border</item>
        <item name="strokeWidth">1dp</item>

    </style>

    <!-- BOTÓN MODERNO -->

    <style name="AppButtonStyle"
        parent="Widget.MaterialComponents.Button">

        <item name="cornerRadius">14dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/white</item>

    </style>

</resources>